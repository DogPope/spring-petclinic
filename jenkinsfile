Jenkinsfile (Declarative Pipeline)
/* Requires the Docker Pipeline plugin */
node {
    stage('build'){
        ./gradlew build
    }
    stage('test'){
        ./gradlew test
    }
    stage('deploy'){
        echo "Hello Deployment."
    }
}